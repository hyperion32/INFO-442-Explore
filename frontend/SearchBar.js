import { React, Component } from 'react'

class SearchBar extends Component {

    // search bar for all pages. Takes in the phrase being searched for along
    // with the list being searched in (categories or list of groups within a category)
    // and returns a list of results.
    // input: the search string and the list being searched from
    //        the list is passed in as a prop from whichever page the search bar is being used on
    // output: a list of matching results
    search(searchPhrase, list) {
        return listOfResults;
    } 

    // displays a search bar
    render() {

    }
}